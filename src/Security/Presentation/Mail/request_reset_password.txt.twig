{{ 'shared.label.hello'|trans({}, 'shared') }} {{ firstname }} {{ lastname }},

{{ 'security.email.request_change_password.body.request'|trans({}, 'security') }}

{{ 'security.email.request_change_password.body.use_token'|trans({}, 'security') }}:
- {{ 'security.email.request_change_password.body.token'|trans({}, 'security') }}: {{ token }}

{{ 'shared.label.good_day'|trans({}, 'shared') }}
{{ 'shared.label.name_signature'|trans({}, 'shared') }}
