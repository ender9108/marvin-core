parameters:
    ddd_cqrs_api_platform_cache_timeout: 3600
    
services:
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
    
    EnderLab\DddCqrsApiPlatformBundle\:
        resource: '../src/'

when@dev:
    parameters:
        ddd_cqrs_api_platform_cache_timeout: 1
